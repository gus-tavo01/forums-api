const TopicsRepository = require('../../../repositories/Topics.Repository');

// TODO:
// setup DB
// beforeAll(() => {});

// destroy connection
// afterAll(() => {});

describe('Topics Repository find', () => {
  test('When filters are op, expect to retrieve a paginated result', async () => {
    // Arrange
    const filters = {};

    // Act

    // Assert
  });
});

// describe('Topics Repository findById', () => {});
